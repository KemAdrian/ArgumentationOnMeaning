(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (uniform-length no))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (uniform-length (define (new-boolean))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (chemical silica))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (chemical (define (chemical))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (sclerospongids no))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (sclerospongids (define (new-boolean))))))
(define (sponge)
  (ecological-features (define (ecological-features)
                         (substrate organic-material))))
(define (sponge)
  (ecological-features (define (ecological-features)
                         (substrate (define (substrate))))))
(define (sponge)
  (ecological-features (define (ecological-features)
                         (association yes))))
(define (sponge)
  (ecological-features (define (ecological-features)
                         (association (define (association))))))
(define (sponge)
  (ecological-features (define (ecological-features)
                         (fixation totally))))
(define (sponge)
  (ecological-features (define (ecological-features)
                         (fixation (define (fixation))))))
(define (sponge)
  (ecological-features (define (ecological-features)
                         (location mediterranean))))
(define (sponge)
  (ecological-features (define (ecological-features)
                         (location (define (sea))))))
(define (sponge)
  (ecological-features (define (ecological-features))))
(define (sponge)
  (external-features (define (external-features)
                       (touch velvety))))
(define (sponge)
  (external-features (define (external-features)
                       (touch (define (touch))))))
(define (sponge)
  (external-features (define (external-features)
                       (boring no))))
(define (sponge)
  (external-features (define (external-features)
                       (boring (define (new-boolean))))))
(define (sponge)
  (external-features (define (external-features)
                       (surface even))))
(define (sponge)
  (external-features (define (external-features)
                       (surface (define (surface))))))
(define (sponge)
  (external-features (define (external-features)
                       (crible no))))
(define (sponge)
  (external-features (define (external-features)
                       (crible (define (new-boolean))))))
(define (sponge)
  (external-features (define (external-features)
                       (brush no))))
(define (sponge)
  (external-features (define (external-features)
                       (brush (define (new-boolean))))))
(define (sponge)
  (external-features (define (external-features)
                       (body-size small))))
(define (sponge)
  (external-features (define (external-features)
                       (body-size (define (body-size))))))
(define (sponge)
  (external-features (define (external-features)
                       (macro-debris none))))
(define (sponge)
  (external-features (define (external-features)
                       (macro-debris (define (macro-debris))))))
(define (sponge)
  (external-features (define (external-features)
                       (gemmules no))))
(define (sponge)
  (external-features (define (external-features)
                       (gemmules (define (new-boolean))))))
(define (sponge)
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (anatomy (define (anatomic-features)
             (ornamentation (define (ornamentation))))))
(define (sponge)
  (anatomy (define (anatomic-features))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (acanthose (define (acanthose))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (ornamentation (define (ornamentation))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form tylostyle))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (megas-form))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (max-length 450))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (max-length (define (integer))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (max-length (define (number))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (two-or-more-length no))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (two-or-more-length (define (new-boolean))))))))
(define (sponge)
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow))))))
(define (sponge)
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external (define (colours))))))))
(define (sponge)
  (external-features (define (external-features)
                       (colour (define (colour))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (grow coating))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (grow (define (growing-kind))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (characteristics (define (characters)
                                                          (shaft fusiform))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (characteristics (define (characters)
                                                          (shaft (define (shaft))))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (characteristics (define (characters))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no))))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle (define (new-boolean))))))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (form thick-encrusting))))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (form (define (form))))))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form))))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing))))))
(define (sponge)
  (external-features (define (external-features))))
(define (sponge))
(define (sponge-ontology))
