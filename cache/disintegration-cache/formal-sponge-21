(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (uniform-length no)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (sterraster))
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (chemical silica)
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))
                        (sclerospongids no)))
  (ecological-features (define (ecological-features)
                         (fixation totally)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (consistency firm)
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))))))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (uniform-length no)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (aster))
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (chemical silica)
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))
                        (sclerospongids no)))
  (ecological-features (define (ecological-features)
                         (fixation totally)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating)))
                       (consistency firm)))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (uniform-length no)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (micros))
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (chemical silica)
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))
                        (sclerospongids no)))
  (ecological-features (define (ecological-features)
                         (fixation totally)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (consistency firm)
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))))))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (uniform-length no)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (chemical silica)
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))
                        (sclerospongids no)))
  (ecological-features (define (ecological-features)
                         (fixation totally)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating)))
                       (consistency firm)))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (uniform-length (define (new-boolean)))
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (chemical silica)
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))
                        (sclerospongids no)))
  (ecological-features (define (ecological-features)
                         (fixation totally)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (consistency firm)
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))))))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (chemical silica)
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))
                        (sclerospongids no)))
  (ecological-features (define (ecological-features)
                         (fixation totally)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating)))
                       (consistency firm)))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (chemical (define (chemical)))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))
                        (sclerospongids no)))
  (ecological-features (define (ecological-features)
                         (fixation totally)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (consistency firm)
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))))))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))
                        (sclerospongids no)))
  (ecological-features (define (ecological-features)
                         (fixation totally)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating)))
                       (consistency firm)))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))
                        (sclerospongids (define (new-boolean)))))
  (ecological-features (define (ecological-features)
                         (fixation totally)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (consistency firm)
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))))))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (ecological-features (define (ecological-features)
                         (fixation totally)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating)))
                       (consistency firm)))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (ecological-features (define (ecological-features)
                         (fixation (define (fixation)))
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (consistency firm)
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))))))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (ecological-features (define (ecological-features)
                         (location atlantic)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating)))
                       (consistency firm)))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (ecological-features (define (ecological-features)
                         (location (define (sea)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (consistency firm)
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))))))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (ecological-features (define (ecological-features)))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating)))
                       (consistency firm)))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (consistency firm)
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))))))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating)))
                       (consistency (define (consistency)))))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))))))
  (anatomy (define (anatomic-features)
             (ornamentation smooth-ornamentation))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating)))))
  (anatomy (define (anatomic-features)
             (ornamentation (define (ornamentation))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))))))
  (anatomy (define (anatomic-features))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        microstrongyle
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        (define (microrhabd))
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (set)
                                        (define (micros))
                                        (define (oxyaster)
                                         (two-length-categories no))))
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (oxyaster)
                                        (two-length-categories no)))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (oxyaster)
                                        (two-length-categories (define (new-boolean)))))
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (oxyaster)))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (aster)))
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (microscleres (define (micros)))
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation smooth-ornamentation)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (ornamentation smooth-ornamentation)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (ornamentation (define (ornamentation)))
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (acanthose no-acanthose)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (acanthose (define (acanthose)))
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length 850)
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length (define (integer)))
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (max-length (define (number)))
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (two-or-more-length no)
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (two-or-more-length (define (new-boolean)))
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (predominant-kind oxeas)))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))
                                       (predominant-kind (define (predominancy)))))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external yellow)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))))))
  (external-features (define (external-features)
                       (colour (define (colour)
                                 (external (define (colours)))))
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))))))
  (external-features (define (external-features)
                       (colour (define (colour)))
                       (growing (define (growing)
                                  (grow coating)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))))))
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet)))
                                  (grow coating))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))))))
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (grow (define (growing-kind)))
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      strongyle
                                                      dichotriaena))))))
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (set)
                                                      (define (megas-form))
                                                      dichotriaena))))))
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form dichotriaena)))))
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no)
                                               (form sheet))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (triaena))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres)
                                       (smooth-form (define (megas-form))))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton)
                        (megascleres (define (megascleres))))))
(define (sponge)
  (spiculate-skeleton (define (spiculate-skeleton))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle no))))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (peduncle (define (new-boolean))))))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (form sheet))))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form)
                                               (form (define (form))))))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing)
                                  (line-form (define (line-form))))))))
(define (sponge)
  (external-features (define (external-features)
                       (growing (define (growing))))))
(define (sponge)
  (external-features (define (external-features))))
(define (sponge))
(define (sponge-ontology))
