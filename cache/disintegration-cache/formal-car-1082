(define (car-description)
  (buying med))
(define (car-description)
  (buying (define (degree-sort))))
(define (car-description)
  (lug_boot small))
(define (car-description)
  (lug_boot (define (degree-sort))))
(define (car-description)
  (persons "2"))
(define (car-description)
  (persons (define (symbol))))
(define (car-description)
  (safety high))
(define (car-description)
  (safety (define (degree-sort))))
(define (car-description)
  (maint med))
(define (car-description)
  (maint (define (degree-sort))))
(define (car-description)
  (doors "2"))
(define (car-description)
  (doors (define (symbol))))
(define (car-description))
(define (car-ontology))
